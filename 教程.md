# BlueSky的八股文 - 使用教程

## 项目简介

这是一个基于 **VuePress 2** + **VuePress Theme Hope** 主题构建的 Java 面试八股文博客网站。

- 网站地址：https://mister-hope.github.io/bluesky-java-interview/
- GitHub 仓库：https://github.com/HuangJiaxin8/bluesky-java-interview

## 目录结构说明

当前仓库 `main` 分支存放的是**构建后的静态文件**（HTML/CSS/JS），用于部署到 GitHub Pages。

要编辑文章，需要创建 VuePress 源项目，目录结构如下：

```
bluesky-java-interview-source/
├── docs/                          # 文章源文件目录
│   ├── .vuepress/
│   │   └── config.ts              # VuePress 配置文件
│   ├── README.md                  # 首页
│   ├── java/
│   │   ├── README.md              # Java 分类首页
│   │   ├── Java基础.md
│   │   ├── Java集合容器.md
│   │   ├── JVM.md
│   │   ├── MySQL.md
│   │   ├── Redis.md
│   │   ├── Spring.md
│   │   ├── MQ.md
│   │   ├── Linux.md
│   │   └── 多线程与并发.md
│   └── ...
├── package.json
└── ...
```

## 环境准备

### 1. 安装 Node.js

访问 https://nodejs.org/ 下载并安装 Node.js（推荐 v18 或更高版本）。

安装完成后，打开终端验证：

```bash
node -v    # 应显示 v18.x.x 或更高
npm -v     # 应显示 9.x.x 或更高
```

### 2. 安装 Git

访问 https://git-scm.com/ 下载并安装 Git。

## 创建 VuePress 源项目

### 方法一：从零开始搭建

```bash
# 1. 创建项目目录
mkdir bluesky-java-interview-source
cd bluesky-java-interview-source

# 2. 初始化 package.json
npm init -y

# 3. 安装 VuePress 和主题
npm install vuepress@next vuepress-theme-hope@next

# 4. 创建 docs 目录结构
mkdir -p docs/.vuepress
mkdir -p docs/java
```

### 方法二：使用脚手架（推荐）

```bash
# 使用官方脚手架创建项目
npm init vuepress@next bluesky-java-interview-source

# 按提示选择配置：
# - 选择 TypeScript 或 JavaScript
# - 选择 VuePress Theme Hope 主题
```

## 配置文件

### docs/.vuepress/config.ts

```typescript
import { defineUserConfig } from "vuepress";
import { hopeTheme } from "vuepress-theme-hope";

export default defineUserConfig({
  base: "/bluesky-java-interview/",
  lang: "zh-CN",
  title: "BlueSky的八股文",
  description: "Java面试八股文整理",

  theme: hopeTheme({
    navbar: [
      { text: "Blog Home", link: "/" },
      {
        text: "文章",
        children: [
          { text: "Java基础", link: "/java/Java基础" },
          { text: "Java集合容器", link: "/java/Java集合容器" },
          { text: "JVM", link: "/java/JVM" },
          { text: "MySQL", link: "/java/MySQL" },
          { text: "Redis", link: "/java/Redis" },
          { text: "Spring", link: "/java/Spring" },
          { text: "MQ", link: "/java/MQ" },
          { text: "Linux", link: "/java/Linux" },
          { text: "多线程与并发", link: "/java/多线程与并发" },
        ],
      },
    ],

    sidebar: {
      "/java/": [
        {
          text: "Java",
          children: [
            "Java基础",
            "Java集合容器",
            "JVM",
            "MySQL",
            "Redis",
            "Spring",
            "MQ",
            "Linux",
            "多线程与并发",
          ],
        },
      ],
    },

    // 主题配置...
  }),
});
```

## 编写文章

### Markdown 文件格式

在 `docs/java/` 目录下创建 `.md` 文件。每个文件开头需要添加 frontmatter：

```markdown
---
title: Java基础
author: blueSky
date: 2023-08-08
category: Java
tags:
  - java基础
  - 面试
---

# Java基础

## 你怎么理解抽象类和接口的

- **抽象类**：对于某类事务进行抽象，比如猫狗可以抽象出一个动物类，是自下而上
- **接口**：定义一些规范，实现该接口的就要按规范来，是自上而下的

## Java是编译型语言和解释型语言

- **编译**：主要是体现在把 Java 代码编译成字节码
- **解释**：主要体现在字节码解释为计算机可识别的机器码
```

### 常用 Markdown 语法

#### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

#### 列表

```markdown
- 无序列表项 1
- 无序列表项 2
  - 嵌套列表项

1. 有序列表项 1
2. 有序列表项 2
```

#### 代码块

````markdown
```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
```
````

#### 表格

```markdown
| 特性 | 说明 |
| --- | --- |
| 速度 | 快 |
| 内存 | 占用少 |
```

#### 图片

```markdown
![图片描述](https://example.com/image.png)
```

#### 链接

```markdown
[链接文字](https://example.com)
```

## 本地预览

```bash
# 启动开发服务器
npm run docs:dev

# 或者使用 vuepress 命令
npx vuepress dev docs
```

启动后访问 http://localhost:8080/bluesky-java-interview/ 预览网站。

修改 Markdown 文件后，浏览器会自动热更新。

## 构建与部署

### 1. 构建静态文件

```bash
# 构建
npm run docs:build

# 或者
npx vuepress build docs
```

构建产物默认输出到 `docs/.vuepress/dist/` 目录。

### 2. 部署到 GitHub Pages

#### 方法一：手动部署

```bash
# 1. 进入构建产物目录
cd docs/.vuepress/dist

# 2. 初始化 git 并推送到 main 分支
git init
git add .
git commit -m "deploy"
git push -f git@github.com:HuangJiaLxin8/bluesky-java-interview.git main
```

#### 方法二：使用 GitHub Actions 自动部署

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy VuePress

on:
  push:
    branches: [source]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Build VuePress
        run: npm run docs:build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/.vuepress/dist
```

#### 方法三：使用 VuePress 官方部署插件

```bash
npm install -D vuepress-plugin-live2d
```

在 `package.json` 中添加脚本：

```json
{
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy": "npm run docs:build && gh-pages -d docs/.vuepress/dist"
  }
}
```

然后运行：

```bash
npm run deploy
```

## 现有文章内容

以下是当前网站包含的文章主题：

| 文件名 | 主题 |
| --- | --- |
| Java基础.md | 抽象类、接口、泛型、反射等 |
| Java集合容器.md | Collection、Map 继承关系 |
| JVM.md | 对象创建、内存分配、垃圾回收 |
| MySQL.md | 索引、事务、SQL 优化 |
| Redis.md | 数据类型、缓存、持久化 |
| Spring.md | IOC、AOP、Bean 生命周期 |
| MQ.md | 消息队列、生产者消费者模式 |
| Linux.md | 常用命令、进程管理 |
| 多线程与并发.md | 线程、锁、并发工具 |

## 常见问题

### Q1: 如何添加新文章？

1. 在 `docs/java/` 目录下创建新的 `.md` 文件
2. 在 `config.ts` 的 `navbar` 和 `sidebar` 中添加导航链接
3. 重新构建并部署

### Q2: 如何修改网站标题？

编辑 `docs/.vuepress/config.ts` 中的 `title` 字段：

```typescript
export default defineUserConfig({
  title: "你的新标题",
  // ...
});
```

### Q3: 如何修改网站图标？

将新的 `favicon.ico` 和 `logo.svg` 放到 `docs/.vuepress/public/` 目录下。

### Q4: 构建时报错怎么办？

1. 确保 Node.js 版本 >= 18
2. 尝试删除 `node_modules` 和 `package-lock.json` 后重新安装：
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

### Q5: 如何在文章中插入代码高亮？

使用三个反引号包裹代码，并指定语言：

````markdown
```java
// Java 代码会被高亮
public class Example {
    private String name;
}
```

```sql
-- SQL 代码会被高亮
SELECT * FROM users WHERE id = 1;
```
````

## 参考资源

- [VuePress 官方文档](https://vuepress.vuejs.org/zh/)
- [VuePress Theme Hope 文档](https://theme-hope.vuejs.press/zh/)
- [Markdown 语法指南](https://www.markdownguide.org/basic-syntax/)

## 技术栈

- **VuePress**: 2.0.0-beta.66
- **主题**: VuePress Theme Hope
- **部署**: GitHub Pages
- **Node.js**: >= 18
