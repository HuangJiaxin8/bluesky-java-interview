---
title: MySQL
date: 2026-09-08
category: 数据库
tags:
  - MySQL
  - 面试

---

# 基础

## 数据库的三范式是什么

- 第一范式：列不可拆分，即原子性
- 第二范式：行记录唯一，即唯一性
- 第三范式：不可派生，即如果两个列通过计算可以得到第三列，那么不需要设置第三列。
- 备注：第三范式已经过时，有时候为了效率多设置一个列，可以牺牲第三范式，达到空间换时间的目的

## MySQL 支持哪些存储引擎?

- 说说常见的innodb和myisam吧，说说他们的区别
- 区别：
    - innodb支持事务、主键、外键，myisam不支持
    - innodb支持行级锁和表级锁，而myisam只支持表级锁
    - myisam内部有存储表行数的变量，而innodb没有

## ​SQL 约束有哪几种

- NotNull，unique，主键，外键，check

## MySQL 中的 varchar 和 char 有什么区别？

- char是定长的字符，比如我们申请了char(10),无论插入多长数据都占用10个字符、
- varchar是可变长的字符，数字代表的是最大字符数，实际占用是实际长度+1，最后一个字符记录的是字符长度
- 速度上看，char>varchar。

## MySQL中 in 和 exists 区别

```

`EXISTS`操作符通常用于存在性子查询，它用于检查子查询是否至少返回一行数据


SELECT *
FROM table1
WHERE EXISTS (SELECT 1
              FROM table2
              WHERE table1.id = table2.foreign_id);

SELECT *
FROM table1
WHERE NOT EXISTS (SELECT 1
                  FROM table2
                  WHERE table1.id = table2.foreign_id);


```


## drop、delete与truncate的区别

- delete是DML语言，所以支持事务，drop和truncate是DDL语言，语句不支持事务
- drop是删除整张表，truncate删除全部数据，不删除表结构，而delete可以删除部分数据。
- 速度上看，drop>truncate>delete

## 什么是存储过程？有哪些优缺点？

- 预编译的SQL语句
- 但不推荐使用，不好维护和迁移

## MySQL 执行查询的过程

- 客户端TCP链接到连接器，这个阶段会做身份校验和资源分配
- 查缓存
- 语法分析，检查SQL语句是否写错了，表和表数据是否存在，别名是否有歧义等
- 优化，是否使用了索引
- 执行器执行，把结果集查询出来然后保存到缓存

## MySQL常用的函数有哪些

- avg()平均数
- sum()总数
- max()最大
- min()最小
- abs() 绝对值
- concat('a','b')
- replace(str,old,new)

## 查询语句不同元素执行先后顺序？

- where
- group by
- having
- select
- distinct
- order by
- limit

## count 性能哪种更好

- count(*) = count(1) > count(主键) > count （字段）
- count(1) :
    - 如果表里只有主键索引，没有二级索引时，那么，InnoDB 循环遍历聚簇索引。
    - 如果有二级索引，就遍历二级索引。
- count(`*`)
    - 其实等于 count(`0`)，也就是说，当你使用 count(`*`) 时，MySQL 会将 `*` 参数转化为参数 0 来处理。
    - count(*) 执行过程跟 count(1) 执行过程基本一样的
- count(主键)
    - 如果表里只有主键索引，没有二级索引时，那么，InnoDB 循环遍历聚簇索引。
    - 如果有二级索引，就遍历二级索引。
- count(普通字段)
    - 全表扫描，最慢

## having和where 有什么区别

- where是在结果集返回之前的过滤，having是在结果集返回之后的过滤。
- having通常配合group by一起使用，用于分组后的过滤
- where 不能使用聚合函数以及聚合函数的别名，having可以
    - select goods_category_id , avg(goods_price) as ag from sw_goods group by goods_category having ag > 1000
    - select goods_category_id , avg(goods_price) as ag from sw_goods where ag>1000 group by goods_category //报错！！因为from sw_goods 这张数据表里面没有ag这个字段

## union 和 union all 有什么区别

- union 对于重复的记录会去重，union all不会
- union默认会按照字段的顺序排序，union all 不会
- 结论：UNION去重且排序，UNION ALL不去重不排序

## left join ，right join，inner join的区别

- left join 返回左表的所有记录和右边匹配到的记录
- right join 返回右表的所有记录和左表匹配到的记录
- inner join 返回左右两张表共同匹配的部分


## 如何优化left join

首先，left join左表是驱动表，右表是被驱动表，left join的执行过程是遍历left join，每次遍历都要扫描一遍右边找到匹配on条件的数据行，然后把匹配的行都放到数据集里。
- 从执行原理看，由于左表遍历每次都要扫描右表，所以如果右表的on条件有索引，会快很多。
- 尽量先过滤掉右边的数据，减少遍历次数。也就是尽量把过滤条件放在 `LEFT JOIN` 之前。



## TIMESTAMP 与 DATETIME 的区别

- 相同点：
    - TIMESTAMP 列的显示格式与 DATETIME 列相同。格式为YYYY-MM-DD HH:MM:SS。
- 不同点：
    - TIMESTAMP：
        - 4个字节存储，时间范围：1970-01-01 08:00:01~2038-01-19 11:14:07
        - 值以UTC格式保存，涉及时区转化，存储时对当前的时区进行转换，检索时再转换回当前的时区
    - DATETIME
        - 8个字节存储，时间范围：1000-10-01 00:00:00~9999-12-31 23:59:59
        - 实际格式存储，与时区无关

# 索引


## MySQL有哪几种索引类型？

- 普通索引
- 唯一索引
- 联合索引
- 聚簇索引
- 非聚簇索引
- 全文索引

## 说一说索引的底层实现？

- 说我们常见的B+树索引吧
- B+树数据都在叶子节点上，并且增加了顺序访问指针，每个叶子节点都指向相邻的叶子节点的地址。
- 进行范围查找时只需要查找两个节点，进行遍历即可。

## B树和B+树的区别

- B树内部节点和叶子节点都存放数据，而B+树内部节点存放的是key值，叶子节点才存放数据
- B+树叶子节点有顺序访问指针，B树没有

## 为什么索引结构默认使用B+Tree，而不是B-Tree，Hash，二叉树，红黑树？

- 不是BTree：
    - B+树 高度低，比较矮胖，能够减少IO
    - 数据都存在叶子节点，方便扫库，适用于范围查询。B树分支节点都存储数据，每次扫库的时候都要通过中序遍历来扫。
- Hash：
    - 没有顺序，天然不支持范围查询
    - 更适用于等值运算
    - 重复数据多，哈希碰撞严重，效率会很低
- 二叉树：
    - 树高度不平衡，不能自平衡，IO代价高
- 红黑树：
    - 高度随数据量的增加而增加，IO代价高

## 讲一讲聚簇索引与非聚簇索引？

- [https://juejin.cn/post/6844903845554814983](https://juejin.cn/post/6844903845554814983)

## 非聚簇索引一定会回表查询吗？

- 不一定，如果是覆盖索引就不会回表查询

## 联合索引是什么？为什么需要注意联合索引中的顺序？

- 联合索引(A,B,C),按A排序，A相等，按B排序，B相等，按C排序



## 讲一讲前缀索引

- 有些字段非常长，但是想建立索引，我们可以提取公共部分作为索引，这样速度会快很多
- 但是，我们需要注意，order by不支持前缀索引 。
- 步骤：
    - 先计算完整列的选择性
    - 再计算不同前缀长度的选择性
    - 找到最优长度之后，创建前缀索引

## 了解索引下推吗？

[Mysql性能优化：什么是索引下推？ - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/121084592)

[五分钟搞懂MySQL索引下推 - 三分恶 - 博客园 (cnblogs.com)](https://www.cnblogs.com/three-fighter/p/15246577.html)

- 举个例子，有个user表，字段id，name，age，address。该表有个复合索引(name,age)
- 有个sql语句：select * from user where name like 陈% and age =20 ，
- 没有索引下推的时候，会先查找非聚簇索引，查出符合 陈开头的所有索引行，然后再去查聚簇索引的所有数据，然后再过滤掉age 不等于20的数据
- 开启索引下推的时候，查找非聚簇索引，查出陈开头的所有行，然后继续判断该索引有没有age的字段，有的话，继续过滤掉age不等于20的索引行。然后再去查聚簇索引的数据。
- 5.6引入了索引下推，默认开启，可以通过配置关闭
- 优点：
    - 可以减少回表次数
    - 在innodb只针对二级索引有效


## 覆盖索引

覆盖索引查询的值刚好是索引字段
比如 select name from user ，而刚好有name这个字段的二级索引，就用二级索引就行了，不需要回表。


## 为什么官方建议使用自增长主键作为索引？

- 减少B+树的分裂和移动的频率。


## 创建索引时需要注意什么？

- 尽量建立联合索引，常用的字段放前面，区分度越高的字段放越前


## 建索引的原则有哪些？

- 最左匹配原则，其中=和in可以乱序
- 尽量使用区分度高的列作为索引
- 索引列不要参与计算
- 尽量扩展索引不要新增索引
- 非空字段指定为NOT NULL约束



## 什么情况下不走索引

- ！ = 和<> 、or，not in 、not exists、 函数、操作运算符
- 类型不一致，发生隐式转换
- 模糊索引%和复合索引没有遵守最左匹配

## 主键与索引有什么区别？

- 主键一定会创建一个唯一索引
- 主键不允许null值，但是唯一索引可以
- 主键是一种约束，唯一索引是一种索引
- 一个表只有一个主键，但是可以多个唯一索引

## 复合索引范围查询右边失效

- mysql中索引底层是B+树，它的叶子节点是有序的，而单列索引中，可以很好的排序。
- 而联合索引中，他是怎么排序的呢？比如联合索引（A,B）,B会依赖于A进行排序，如果如果A进行了范围查询，那么B字段没办法依赖A的排序，导致B无序了。那么它就不能用二分查找法查找到b的值了。

## MySQL的聚簇索引和非聚簇索引

- MySQL的索引是跟存储引擎是相关的
    - innodb中索引跟数据都放在idb文件中
    - myisam的索引放在myi中，数据文件放到myd文件中
- innodb插入数据的时候
    - 会跟主键索引放到一起，是聚簇索引。
    - 其他索引只是存放主键的位置，通过主键的位置去查找数据。

## MySQL的聚簇索引和非聚簇索引

- 聚集索引就是基于主键创建的索引，除了主键，其他索引都叫非聚集索引，也叫二级索引
- 因为在innodb引擎中，一张表的数据对应的物理文件，本身就是按照B+树来组织的，而聚集索引就是按照每张表的组件来构建这样一个B+树，然后叶子节点存储了这个表里面的每一行数据记录。
- 所以基于Innodb这样的一个特征，聚集索引并不仅仅是一种索引类型，还代表了一种数据的存储方式，同时也意味着表里必须要有一个主键，如果没有，MySQL会默认给一个隐藏列作为主键。
- 非聚集索引来查询一条数据的时候，由于不存放数据，最终还是要对聚集索引来检索。

## innodb里面只能存在一个聚集索引

- 如果存在多个聚集索引，那么意味着表里面数据会存在多个副本，造成磁盘空间的浪费

## 索引设计原则

- 给查询的列加上索引
- 表的量小就别加索引
- 频繁增删改的字段就别加索引
- 索引的字段长度越短越好，比如可以用前缀索引
- 创建索引的列不要太多，可以用组合索引
- 大文本、大对象不要创建索引

## MySQL 普通索引和唯一索引哪个读写性能高一点

- 读性能：唯一索引略快，因为是唯一的，读到一个直接返回。
- 写性能：唯一索引略慢，因为写入的时候需要保证唯一性，需要额外的性能消耗。

## b+树 三层 最多存多少数据

- 1170 * 1170 * 16 = 21902400
- [https://zhuanlan.zhihu.com/p/379092178](https://zhuanlan.zhihu.com/p/379092178)

# 数据库事务


## 介绍一下事务具有的四个特征

- A：atomicity 原子性，同一事务要么成功要么不成功
- C：consistency 一致性，事务一提交成功，数据库就要处于一致性状态
- I ：isolation 隔离性 ，不同事务之间相互隔离不影响
- D：durability 持久性，事务一旦成功，数据的改变就应该是永久的

## 什么是脏读？幻读？不可重复读？

- 脏读：事务A读到了事务B的数据，然后事务B回滚，那么A读到的就是脏数据。
- 不可重复读：事务A多次读取同一数据，事务B对在事务A多次读取过程中，对数据进行修改，导致事务A读取到的数据不一致
- 幻读：跟不可重复度类似，不过不可重复读针对的是数据的修改，幻读针对的是数据行的增加删除。

## 事务的实现原理

- 基于redo log（重做日志）和undo log（回滚日志）实现
- 每提交一个事务必须先将该事务的所有日志写入到重做日志文件进行持久化，数据库就可以通过重做日志来保证事务的原子性和持久性。
- 每当有修改事务时，还会产生 undo log，如果需要回滚，则根据 undo log 的反向语句进行逻辑操作，比如 insert 一条记录就 delete 一条记录。undo log 主要实现数据库的一致性。

## MySQL 的事务 ACID 是通过什么方式实现的

- 原子性是通过undo log 实现的，需要回滚的时候，撤销执行成功的SQL
- 一致性是由其他三大特性解决的
- 隔离性是通过MVCC控制的
- 持久性是通过redo log保证的，mysql修改数据的时候会在redo log记录一份日志，就算数据没有保存，日志保存成功，数据也不会丢失

## MySQL 如何实现原子性

- 原子性是通过undo log实现，undo log 也叫回滚日志，但需要回滚的时候，那么就可以把undo log里面的语句记录做一下反向操作就行了。
    - 比如，insert 一条语句就delete 一条语句；update新值就update旧值。

# 日志文件

## [什么是MySQL的 binlog？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_7-%E4%BB%80%E4%B9%88%E6%98%AFmysql%E7%9A%84-binlog%EF%BC%9F)(用于主从复制)

- bin log记录的是表结构和表数据变化，不会记录select和show的语句。
- 事件形式记录，包含语句所耗时，主要目的复制和恢复。
- binlog有三种格式：
    - statement 基于SQL语句
    - row 基于行，记录的是行变化
    - mixed 混合模式，根据情况来选择statement还是row

## 三个日志说明

 ![](attachments/Pasted%20image%2020240224165108.png)
![](attachments/Pasted%20image%2020240224165118.png)

![](attachments/Pasted%20image%2020240224165130.png)


- Redo Log
	- 持久化日志，记录一些修改数据的操作，数据库系统奔溃会读取该日志文件，把没有落盘的操作落盘
- bin log
	- 也是记录一些表结构和数据修改的操作，主要是用于
		- 数据复制（主从）
- undo log
	- 用于回滚：记录一下对表数据修改的逆向操作，比如insert语句就对应delete
	- MVCC:记录上一版本的数据快照


## MySQL的二阶段提交

参考：https://zhuanlan.zhihu.com/p/343449447

![](attachments/Pasted%20image%2020240205195358.png)

- MySQL的二阶段提交是指写redo log 分为2个阶段去写
	- 第一阶段叫预提交redo log
	- 预提交之后写bin log
	- bin log 写完之后就正式提交redo log （这步是第二阶段提交）
- MySQL 的二阶段提交是为了保证redo log 和 bin log的一致性
- 如果2个动作都独立写，就没办法保证一致性，有可能写完redo log之后就发生故障，导致bin log 没写，而redo log 也没办法回滚


## MySQL的架构

![](attachments/Pasted%20image%2020240205200451.png)

- 主要分为服务层和存储引擎层
- 服务层包括：
	- 连接池
	- 系统管理和控制工具：比如权限校验等安全管理，集群管理，备份恢复等
	- sql接口：接收sql命令和返回结果
	- 解析器：解析语法树，并判断sql是否合法
	- 查询优化器：优化sql的工具
	- 缓存：缓存机制，8.0版本默认关闭
- 引擎层
	- 可供可插拔的存储引擎，与系统文件交互



## 在事务中可以混合使用存储引擎吗

- 尽量不要同一个事务使用到不同的存储引擎
- 如果一个事务中使用了事务型和非事务型的表，那么事务正常提交没有问题
- 但是要是事务发生回滚，会导致事务型的表回滚了，非事务型的表没有回滚，导致数据库处于不一致状态


# MVCC

## 什么是 MVCC

- MVCC多版本并发控制。
- InnoDB 每一行数据都有一个隐藏的指针，指向上一个历史版本，这个历史版本存放在 undo log 中。
- 一个事务在执行更新操作时，其它事务此时需要查询时，会查询上一个历史版本记录。这样保证了读写不冲突，提高了Mysql并发性能。
- ReadView是MVCC中的一个数据结构，用于确定一个事务可以看到哪个版本的数据。在RR隔离级别下，事务开始的时候会创建，记录了当前事务id，当前系统中活跃的事务id列表，活跃事务中的最小事务id。


##  ReadView规则

- ReadView主要包含四个数据：
	- creator_trx_id:创建该readview的事务id，
	- trx_ids:创建ReadView时活跃中的事务id列表，
	- up_limit_id:创建ReadView时活跃事务中的最小事务id
	- low_limit_id:生成ReadView时，系统应该分配给下一个事务的id
- 注意: low_limit_id并不是trx_ids中的最大值，事务id是递增分配的。比如，现在有id为1，2，3这三个事务，之后id为3的事务提交了。那么一个新的读事务在生成ReadView时，trx_ids就包括1和2，up_limit_id的值就是1，low_limit_id的值就是4。

- 规则:
	- 访问某条记录的时候，记录的trx_id小于等于ReadView的最小事务id，那么可以访问，反之就不能访问
	- 如果被访问版本的trx_id属性值在ReadView的up_limit_id和low_limit_id之间，那就需要判断一下trx_id属性值是不是在trx_ids列表中。
		- 如果在，说明创建ReadView时生成该版本的事务还是活跃的，该版本不可以被访问
		- 如果不在，说明创建ReadView时生成该版本的事务已经被提交，该版本可以被访问


> https://juejin.cn/post/7154701807694905351

## RC和RR下，MVCC有区别吗？

RR隔离级别下，ReadView是在事务开始的时候创建的。
RC隔离级别下，ReadView是在每个语句执行时都会生成，意味着还是有不可重复读和幻读的问题。



# 事务隔离级别

## 说一下MySQL 的四种隔离级别

- 读未提交：会产生 脏读、不可重复读、幻读
- 读已提交 RC：会产生 不可重复度，幻读
- 可重复读 RR：会产生 幻读，MySQL默认的隔离级别，但MySQL通过mvcc解决幻读
- 串行化：强制事务之间以顺序的形式执行，不会有并发事务问题

## MySQL中是如何实现事务隔离的?

- 本来可以选择是串行化的隔离级别，这样就不会有幻读问题，但是这种隔离级别相当于单线程，效率低。
- MySQL使用了可重复读，通过行锁+间隙锁的组合next-key实现，解决了幻读问题。

## MySQL默认隔离级别是可重复读，它会有幻读吗？

- SELECT查询分为快照读和实时读
- 快照读：
    - 通过MVCC来解决，MVCC是多版本并发控制，通过快照，一个事务在做更新操作的时候，另一个事务在读取的时候，读的是上一个版本的数据
- 实时读：
    - 通过Next-Key Lock来解决幻读问题。
    - Next-Key Lock是Gap Lock（间隙锁）和Record Lock（行锁）的结合版。
    - 比如，select * from id>100 for update
        - 会给主键id=100的记录加上record行锁
        - 主键id会加上gap锁，锁住id(100,+无穷大）的范围
        - 其他事务对id>=100范围的记录读和写操作都将被阻塞
        - 这样就可以阻止其他事务修改该范围的数据，保证了不会出现幻读

## MySQL在可重复读隔离级别是怎么解决不可重复读问题的？


（引入MVCC机制读快照，解决了不可重复读的问题）
- 引入了MVCC机制，如果是快照读情况下，读的是快照，自然不会有不可重复读问题
- 实时读，通过间隙锁避免了幻读。

## MySQL的innodb默认隔离级别为什么要设置成可重复读

- 数据一致性与并发性的权衡
- innodb引入了间隙锁也解决了幻读问题


## 为啥像阿里巴巴这种公司为什么推荐MySQL的隔离级别设置为RC?

因为为了提高MySQL的性能，属于性能与不可重复读、幻读问题上做的权衡。
首先rc隔离级别一般情况是没有gap lock，性能自然比rr好很多。
而且不可重复读和幻读发生的概率极低，正常业务都不会在一个事务下读同一个sql语句。



# 锁

## [为什么要加锁?](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_1-%E4%B8%BA%E4%BB%80%E4%B9%88%E8%A6%81%E5%8A%A0%E9%94%81)

- 多用户环境保证数据库完整性和一致性
- 当多个用户并发存取数据时，在数据库就会产生多个事务存取同一数据的情况。如果对并发操作不加以控制可能会导致数据不正确，破坏一致性。

## [按照锁的粒度分数据库锁有哪些？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_2-%E6%8C%89%E7%85%A7%E9%94%81%E7%9A%84%E7%B2%92%E5%BA%A6%E5%88%86%E6%95%B0%E6%8D%AE%E5%BA%93%E9%94%81%E6%9C%89%E5%93%AA%E4%BA%9B%EF%BC%9F)

- 行级锁：Mysql中粒度最细的锁，发生冲突概率低，并发高，但开销大。行级锁分为排它锁和共享锁。
- 表级锁：Mysql锁粒度最大，对整张表加锁，开销小，不会出现死锁，发生冲突的概率最大，并发度低。
- 页级锁：介于表级锁和行级锁之间的一种锁

## [从锁的类别上分MySQL都有哪些锁呢？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_3-%E4%BB%8E%E9%94%81%E7%9A%84%E7%B1%BB%E5%88%AB%E4%B8%8A%E5%88%86mysql%E9%83%BD%E6%9C%89%E5%93%AA%E4%BA%9B%E9%94%81%E5%91%A2%EF%BC%9F)

- 锁的类别上分的话，分为共享锁和排它锁
    - 共享锁：又叫读锁。读数据的时候，加上共享锁，可以多加几个。
    - 排他锁：又叫写锁。写数据的时候，加上排他锁，只可以加一个，与其他排他锁，共享锁互斥。

## [数据库的乐观锁和悲观锁是什么？怎么实现的？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_4-%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84%E4%B9%90%E8%A7%82%E9%94%81%E5%92%8C%E6%82%B2%E8%A7%82%E9%94%81%E6%98%AF%E4%BB%80%E4%B9%88%EF%BC%9F%E6%80%8E%E4%B9%88%E5%AE%9E%E7%8E%B0%E7%9A%84%EF%BC%9F)

- 乐观锁和悲观锁是数据库并发控制中采用的主要技术手段
- 乐观锁：假设不会发生并发冲突，只是提交的时候检查一下数据的完整性，修改的时候给事务上锁，通过version的方式进行锁定。实现的方式有：CAS或加上版本号。
- 悲观锁：假设会发生并发冲突，屏蔽一切可能会违反数据完整性的一切操作。在查询完数据的时候就把事务锁起来，直到提交事务。实现方式：数据库锁机制。

## [InnoDB引擎的行锁是怎么实现的？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_5-innodb%E5%BC%95%E6%93%8E%E7%9A%84%E8%A1%8C%E9%94%81%E6%98%AF%E6%80%8E%E4%B9%88%E5%AE%9E%E7%8E%B0%E7%9A%84%EF%BC%9F)

- 基于索引来实现的，如果sql语句不走索引，那么锁升级会表锁

## [什么是死锁？怎么解决？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_6-%E4%BB%80%E4%B9%88%E6%98%AF%E6%AD%BB%E9%94%81%EF%BC%9F%E6%80%8E%E4%B9%88%E8%A7%A3%E5%86%B3%EF%BC%9F)

- 死锁是两个事务，相互占用对方的资源却相互请求，导致恶性循环的现象。
- 一个有趣的例子：
    - 面试官：如果你能回答什么是死锁，我就录取你。
    - 面试者：如果你录取我，我就给你回答什么是死锁。
- 常见的方式：
    - 如果不同程序会存取多个表，尽量约定以相同的顺序访问表。
    - 在同一个事务，尽可能做到一次锁定所有的请求资源。
    - 对于非常容易死锁的业务，可以通过锁升级为表锁的方式。

## [隔离级别与锁的关系](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_7-%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB%E4%B8%8E%E9%94%81%E7%9A%84%E5%85%B3%E7%B3%BB)

- Read Uncommitted 读数据不加共享锁
- Read committeed 读数据加共享锁，读取完成后释放
- Repeatable Read 读数据加排他锁，事务完成后释放
- SERIALIZABLE 锁定整个范围的键，事务完成后释放

## 优化锁方面的意见？

- 使用较低的隔离级别
- 设计索引，尽量使用索引去访问数据
- 合理选择事务的大小，给记录显示加锁，最后一次性请求足够多的资源
- 数据查询的时候，不是非必要，不要上锁。MySQL的MVCC可以实现事务中查询不用加锁，优化事务性能。注意：MVCC 只在Read Commiteed 和 Repeatable Read 的时候生效。

## 什么是Mysql锁升级？

- MySQL行锁只能加在索引上，如果操作不走索引，那么会升级为表锁。
- 当非唯一索引重复记录超过表记录的一半时候，行锁也会升级为表锁，因为它底层会索引失效，自然不走行锁。

## innodb有哪些锁

- 行锁
    - 行锁分为共享锁和排他锁
    - 共享锁加锁方式：select * from table where xxx for share;
    - 排他锁加锁方式：select * from table where xxx for update;
- 表锁/意向锁
    - 表级锁又叫做意向锁，指示事务稍后对表中的行需要哪种类型的锁（排他锁或共享锁）。所以又分为两种类型的意向锁。
- 记录锁Record Locks ：记录锁是对索引记录的锁定
    - select c1 from t where c1 = 10 for update;
    - 表示防止任何其他事务插入、更新或删除t.c1=10的行。
- 间隙锁Gap Locks ：记录锁是对索引记录的锁定
    - select c1 from t where c1 between 10 and 20 for update;
    - 表示防止其他事务动（插入、更新或删除）t.c1在10～20中的记录。
- 下一个键锁/临界锁 Next-Key Locks
    - 是索引记录上的记录锁和索引记录之前的间隙上的间隙锁的组合。即Next-Key Lock 包含 Record Lock 和 Gap Lock。
    - 默认情况下，InnoDB在可重复读取事务隔离级别下运行。在这种情况下，InnoDB使用next key lock进行搜索和索引扫描，从而防止幻读。
- 插入意向锁Insert Intension Locks
    - 插入意向锁是在行插入之前由插入操作设置的一种间隙锁。
- 自增锁
    - AUTO-INC锁是一种特殊的表级锁，由插入到具有自动增量列的表中的事务使用。
- 数据库如何用到CAS机制
    - 在表中有一个字段value
    - 然后第一次拿的时候，去记录一下第一次拿的value值
    - 执行更新操作的时候，去拿一下value，判断是否改变，如果没改变就直接更新
    - 但是这样会有ABA的问题，我们可以在加一个版本号字段解决

# SQL调优

## SQL调优高级版本

- 工作日常中，总会遇到一些性能问题，而大概率都是SQL的性能问题。我们一般是会给MySQL开启慢查询日志，然后对于这些慢SQL给捞取出来，用explain语句执行一下，看看是否命中索引，如果没有命中索引，那么就加索引。如果命中索引，看看是不是索引不行。一般业务的SQL都是稍微复杂一点的，都是要建联合索引来最大化提高效率，联合索引由于最左匹配原则，要考虑哪个字段的先后顺序，一般来说，区分度越大的放在前面。
- 还有一些说SQL优化的基本准则
	- 不要join超过3张表
	- like和联合索引要遵守最左匹配原则
	- 不要使用select 星 
	- 字符串索引字段要使用双引号

## SQL调优

- 首先禁止使用select *
- 用explian查看sql 语句是否命中索引，没有的话就修改sql，该加索引就加索引
- 避免使用or ，is null ，<> 操作符
- 复合索引和like语句要遵守最左匹配原则
- 字符串字段的话要加双引号

## [​如何定位及优化SQL语句的性能问题](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_1-%E5%A6%82%E4%BD%95%E5%AE%9A%E4%BD%8D%E5%8F%8A%E4%BC%98%E5%8C%96sql%E8%AF%AD%E5%8F%A5%E7%9A%84%E6%80%A7%E8%83%BD%E9%97%AE%E9%A2%98%EF%BC%9F)

- 慢查询日志打开
- explian 一下SQL语句看看是走索引
- 走了索引连接多少表，看看是否能不能小表驱动大表
- 关联的两张表看看能不能在子查询后成为虚拟表再join，减少数据量

## 大表数据查询，怎么优化

- 优化sql语句，加索引
- 加Redis等缓存
- 主从复制，读写分离
- 垂直拆分，水平拆分

## 超大分页怎么处理?

- `select * from table where age > 20 limit 1000000`,10 这种查询其实也是有可以优化的余地的. 这条语句需要 load1000000 数据然后基本上全部丢弃,只取 10 条当然比较慢. 当时我们可以修改为`select * from table where id in (select id from table where age > 20 limit 1000000,10)`.这样虽然也 load 了一百万的数据,但是由于索引覆盖,要查询的所有字段都在索引中,所以速度会很快。

## 统计过慢查询吗？对慢查询都怎么优化过？

- 我们一般sql耗时，一般都会在测试库里面测。慢查询统计主要是运维在做，会定期将业务中的慢查询反馈给我
- 慢首先要分析为什么慢？是不是加载了多余的列，sql语句没走索引，还是数据量太多？
    - 分析一下SQL，看看是不是load多余的列
    - explain下SQL语句，看看是不是没走索引，优化SQL使其尽量走索引
    - 数据量太大的话可以分库分表

## [如何优化查询过程中的数据访问](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_5-%E5%A6%82%E4%BD%95%E4%BC%98%E5%8C%96%E6%9F%A5%E8%AF%A2%E8%BF%87%E7%A8%8B%E4%B8%AD%E7%9A%84%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE)

## [如何优化关联查询](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_6-%E5%A6%82%E4%BD%95%E4%BC%98%E5%8C%96%E5%85%B3%E8%81%94%E6%9F%A5%E8%AF%A2)

## [MySQL数据库cpu飙升到500%的话他怎么处理？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_8-mysql%E6%95%B0%E6%8D%AE%E5%BA%93cpu%E9%A3%99%E5%8D%87%E5%88%B0500%E7%9A%84%E8%AF%9D%E4%BB%96%E6%80%8E%E4%B9%88%E5%A4%84%E7%90%86%EF%BC%9F)

## [大表怎么优化？](https://www.javalearn.cn/#/doc/MySQL/%E9%9D%A2%E8%AF%95%E9%A2%98?id=_9-%E5%A4%A7%E8%A1%A8%E6%80%8E%E4%B9%88%E4%BC%98%E5%8C%96%EF%BC%9F)

- 限定范围查询
- 读写分离
- 分库分表

## 百万级别的数据如何删除

- 关于索引：索引
- 先删除索引（大概三分钟）
- 删除要删除的数据（看要删除的数据量，一般不超过两分钟）
- 重新建立刚删除的索引（数据量相对少了，建索引更快了）

## select a from table where b = 'xxx' group by c 如何建立索引

- 联合索引bca ，bc是为了覆盖索引的同时避免filesort
- 而a是为了避免回表查询
- [https://blog.csdn.net/m0_46405589/article/details/112601638](https://blog.csdn.net/m0_46405589/article/details/112601638)

# 分库分表

## 什么是分库分表？

- 分库是指把一个数据库拆分成多个数据库，一般分为水平分库和垂直分库。
- 分表是指通过一定的规则，把一张表分解成多张不同的表，一般分为水平分表和垂直分表。
    - 水平分表：就是一般意义的去分表，按行去分
    - 垂直分表：把宽表拆成几张细表

## 分库和分表他们解决的问题有什么不一样

- 分库的目标主要解决的问题是：减少单一数据库的负债，提高数据库的并发能力，方便横向扩展。
- 分表解决的问题是：大表会导致性能差的问题（查询性能慢，写入压力大）

## 分区表和分表有什么区别？有啥优缺点

- 分区表还是一张物理表，只是底层的存储文件分成多个，按相应的规则划分
	- .frm文件是同一个(存放表结构)，不同区.ibd文件不同（存放数据）
- 分表是把一张大表拆分多张真实的物理表
- 共同点：都能提高单表的查询性能
- 不同点：
	- 分区表作用在一台MySQL节点上。分表可以一台MySQL节点上分，也可以分库后再分表，坐落再不同节点。
	- 分区表不需要依赖框架或者中间件，而分表需要
	- 分区表本质还是一张表，所以索引维护一份就行了，而分表要每张表都去维护索引保持一致


## 分库分表后如何进行分页查询

- 拿订单的分库分表举例，我们用买家ID分表
	- shardingkey查询
		- 一般来说买家的订单查询是最高频的，对买家来说，就可以直接用买家id来查询，这样就可以直接路由到单个库进行分页查询了
	- 非shardingkey查询
		- 比如卖家也像查询订单，那么可以基于canal把数据同步到以买家拆分的表中，虽然有秒级的误差，但是业务可以接受
		- 大企业，一般都是同步到es、分布式数据仓库上，TiDB、PolarDB、ES等

## 分表数据倾斜如何解决

- 什么是数据倾斜
	- 比如按商户分表，但是某个商户卖的订单比较多，所以销售数据表中，该商户所在的表就比较大

- 数据倾斜的影响
	- 资源利用不均衡：倾斜的节点负载高，其他节点比较闲
	- 影响其他业务：比如某个小商家和一个大商家的数据都在同一张表上，那么它的查询会比较慢
	- 查询效率低下，分表的效果不明显

- 建议不要选择聚集性的字段作为分表字段


- 如何解决？
	- 多级分片
		- 先按第一维度去分，然后再此基础上按第二维度去拆分
	- 隔离
		- 给大表所在的商户单独部署一套系统
		- 常见的作法
	- 拆分
		- 比如redis中，为了解决热key的问题，可以常用cluster模式，把一个大key拆分到多个实例上存储


# 主从复制

## 为什么要主从复制

- 提高Mysql性能，读写分离主写从读，分工，减少单机的压力

## MySQL主从复制的原理

- master节点修改的时候会把记录 记录到bin log
- slave 节点会有一个IO线程去读取master的bin log
- 会在slave 节点生成一份Relay log
- slave节点的SQL 线程会去读取这个Relay log，然后解析成SQL语句在slave库执行一遍

## Cannal的实现原理

- Canal会模拟MySQL slave的交互协议，把自己伪装成一个MySQL slave，向MySQL master发送dump协议。
- MySQL接收到消息后，会向它发送binlog，之后解析这个canal就可以了。

# MySQL架构

## server层和引擎层各自的职责

- Server层：负责处理客户端连接、解析查询请求、执行查询语句、管理事务、安全认证、并发控制以及其他管理任务。
- 存储引擎层负责将数据存储到磁盘上，并处理数据的读取和写入操作。

