

- 在类方法中只能调用本类的类方法
    
    - 错误：因为除了可以直接调用本类类方法，还可以通过创建实例，调用实例方法。
- 除了JRE的JVM不是用java写的，其他工具都是用java写的
    
    - 正确
- Stub（存根） 与哪种技术有关
    
    - 动态链接
    - 存根类是一个类，它实现了一个接口，它的作用是：继承该类则不需要实现一个接口全部方法
- Cookie是Web服务器发送给客户端的一小段信息，客户端请求时，可以读取该信息发送到服务器端
    
    - 正确的，没看清楚
- 包含抽象方法的类称为抽象类，但并不意味着抽象类中只能有抽象方法，它和普通类一样，同样可以拥有成员变量和普通的成员方法。注意，抽象类和普通类的主要有三点区别：
    
    - 抽象方法必须为public或者protected（因为如果为private，则不能被子类继承，子类便无法实现该方法），缺省情况下默认为public。
    - 抽象类不能用来创建对象；
    - 如果一个类继承于一个抽象类，则子类必须实现父类的抽象方法。如果子类没有实现父类的抽象方法，则必须将子类也定义为为abstract类。
    
    在其他方面，抽象类和普通的类并没有区别。 - java接口包含函数声明和变量声明。 - 错误：1.8接口默认public static final ，所以是常量不是变量
    
- errorPage 的意思是设置当前页面要**引入错误的页面**。也就是浮面当前页面如果出现错误就会跳转到errorPage所指定的页面。
    
    - isErrorpage 的意思是当前页面为**错误页面**。
    - **isErrorPage默认值为false**，若要当前页面为错误页面就设置isErrorPage=true。
- 数组属于一种原生类
    
- 基本类型是原生类，但是数组是引用类型，属于对象
    
- 运行时常量池
    
    - 受方法区大小限制
    - 存放了编译时生成的各种字面量
    - 存放了编译时期生成的符号引用
- double d1=-0.5;
    
    System.out.println("Ceil d1="+Math.ceil(d1));
    
    System.out.println("floor d1="+Math.floor(d1));
    
    - 输出：
    - Ceil d1=-0.0
    - floor d1=-1.0
- linux 中：
    
    - $0 shell或[shell脚本](https://www.baidu.com/s?wd=shell%E8%84%9A%E6%9C%AC&tn=44039180_cpr&fenlei=mv6quAkxTZn0IZRqIHckPjm4nH00T1Y3PWnsrAndPHIWP1ndmyDv0ZwV5Hcvrjm3rH6sPfKWUMw85HfYnjn4nH6sgvPsT6KdThsqpZwYTjCEQLGCpyw9Uz4Bmy-bIi4WUvYETgN-TLwGUv3EPjnvPWbdP1R1)的名字
    - $# 参数的个数
- Integer b1 = new Integer_(_2017_)_;
    
    int b2 = 2017;
    
    Integer b3 = 2017;
    
    System._out_.println_(_b1 == b2_)_;
    
    System._out_.println_(_b1 == b3_)_;
    
    - true
    - false
- Web开发实现会话跟踪的有：<input type="hidden" name="session" value="a1234">
    
    - Session
    - Cookie
    - 地址重写
    - 隐藏域：<input type="hidden" name="session" value="a1234">
- 命令行： java Test one two three
    
    - args[0] 代表的是：one
- 下列赋值正确么：
    
    - double d=5.3e12; 正确
    - float f=11.1; 错误，11.1是double类型，需要强制转换才能转为float
- 在方法中定义的局部变量在该方法被执行时创建
    
    - 这是错误的，是方法调用的时候入栈的时候创建的，而不是执行的时候
- 注意：不仅String 被final 修饰，StringBuidler，StringBuffer也是final修饰
    

```text
public class Base
{
   public void methodOne()
   {
      System.out.print("A");
      methodTwo();
   }
 
   public void methodTwo()
   {
      System.out.print("B");
   }
}
 
public class Derived extends Base
{
   public void methodOne()
   {
      super.methodOne();
      System.out.print("C");
   }
 
   public void methodTwo()
   {
      super.methodTwo();
      System.out.print("D");
   }
}

Base b = new Derived（）
调用b.methodOne()会发生什么？

```

答案：ABDC

- 注意：Math是被final修饰的类，Number可以被继承
- CopyOnWriteArrayList 写的时候加锁，读的时候不加锁，所以适用于读多写少的情况
- ReadWriteLock即为读写锁，他要求写与写之间互斥，读与写之间互斥。所以读与读之间不互斥，适用于多读写少的情况。
- ConcurrentHashMap是同步的HashMap，读写都加锁
- 数组复制的速度：System.arraycopy>clone>Arrays.copyOf>for循环
    - [https://www.nowcoder.com/test/question/done?tid=52127770&qid=55429#summary](https://www.nowcoder.com/test/question/done?tid=52127770&qid=55429#summary)
- java在运行时才进行翻译指令
- java编译出来的目标文件，可以运行在任意jvm上 （错误，注意JVM版本）
- java编译出来的目标文件与具体操作系统有关 （错误，class文件与系统无关）
- 正确创建二位数组的语句有：
    - float f[][] = new float[6][6];
    - float []f[] = new float[6][6];
    - float [][]f = new float[6][6];
    - float [][]f = new float[6][];
    - 总结：前面部分可以不看，（大概率编译器会优化）主要看二位数组，第一位有没有数，有的话就是正确的
- PriorityQueue是不同于先进先出队列的另一种队列。每次从队列中取出的是具有最高优先权的元素。
- 数组
    - 数组是一个对象，不同类型的数组具有不同的类
    - 数组是一个连续的存储结构
    - java中不存在 int *a这样的东西做数组的形参
- ThreadLocal是采用哈希表的方式来为每个线程都提供一个变量的副本，解决哈希冲突的方法是开放地址表
- 字符串是对象，调用的是length方法，即str.length()，而不能str.length

```text
链接：https://www.nowcoder.com/questionTerminal/35a08d98faba40e6a8946fcb88c6c091
来源：牛客网

import java.util.Date; 
public class SuperTest extends Date{ 
    private static final long serialVersionUID = 1L; 
    private void test(){ 
       System.out.println(super.getClass().getName()); 
    } 
      
    public static void main(String[]args){ 
       new SuperTest().test(); 
    } 
}
TestSuper和Date的getClass都没有重写，他们都是调用Object的getClass，而Object的getClass作用是返回的是运行时的类的名字。这个运行时的类就是当前类，所以
super.getClass().getName()
返回的是test.SuperTest，与Date类无关
要返回Date类的名字需要写super.getClass().getSuperclass()
注意：Object.getClass()是final ， 是无法重写的
```



```Java
ArrayList<String> strArr = new ArrayList(){{
    add("a");
    add("b");
    add("c");
    add("d");
}};
List<String> strSubList = strArr.subList(0, 2);
strSubList.add(0,"aa");
strSubList.add("e");

System.out.println(strArr);
System.out.println(strSubList);
```